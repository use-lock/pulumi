name: check

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.13.2

      - name: Check release configuration
        uses: goreleaser/goreleaser-action@v6
        with:
          version: v2.18.1
          args: check

      - name: Test
        run: make check

      - name: Verify go.mod and go.sum are tidy
        run: |
          go mod tidy
          git diff --exit-code -- go.mod go.sum

      - name: Install Pulumi CLI
        run: |
          curl -fsSL https://get.pulumi.com | sh -s -- --version 3.261.0
          echo "$HOME/.pulumi/bin" >> "$GITHUB_PATH"

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Verify schema and SDK are up to date
        run: |
          make schema
          test -s schema.json
          make gen-sdk
          # The lockfile differs between npm versions (peer flags); it is not part of the drift check.
          git diff --exit-code -- schema.json sdk/nodejs ':!sdk/nodejs/package-lock.json'
          test -z "$(git ls-files --others --exclude-standard -- sdk/nodejs)"

  sdk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: sdk/nodejs/package-lock.json
      - run: make build-sdk
      - name: Check npm package metadata and documentation
        run: |
          cmp README.md sdk/nodejs/bin/README.md
          cmp LICENSE sdk/nodejs/bin/LICENSE
          node -e 'const assert = require("node:assert/strict"); const pkg = require("./sdk/nodejs/bin/package.json"); assert.equal(pkg.license, "MIT");'
          cd sdk/nodejs/bin
          npm pack --dry-run
      - name: Check TypeScript example
        working-directory: examples/typescript
        run: |
          npm ci --no-audit --no-fund
          npm run check
